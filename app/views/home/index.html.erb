<div class="grid_9" id="news-list">
  <%= will_paginate @links, :next_label => image_tag('/images/right_arrow.png', :class => 'arrow'), :previous_label => image_tag('/images/left_arrow.png', :class => 'arrow')  %>
  
  <div id="news-items">
  <% if (@links.length==0) %>
    There's nothing here.
  <% elsif %>
    <%= render :partial => 'links' %>
  <% end %>
  </div>

</div>

<div id="filter_wrapper" class="grid_3">
	
  <div id="filter">
    <h2>Don't Show</h2>
    <form action="" method="">
      <select id="filter-list">
        <% @filters.each do |id,name| %>
          <option value="<%=id%>"><%=name%></option>
        <% end %>
      </select>
      <input type="submit" value="add" />
    </form>
    <ul id="selected-filters">
      <% @active_filters.each do |id,name| %>
        <li><a id="<%=id%>" href="#"><%=name%></a></li>
      <% end %>
    </ul>
    <p class="help">Add everything you hate to the list above and we'll keep it off your page.</p>
  </div>
<img src="/images/logo.png" alt="Agragr" id="logo" />

	<div id="about">
		<img src="/images/about.png" alt="What is this?" />
		<h3>About</h3>
    <p>Agragr aggregates news that hits the front page on a variety of social news sites. It filters out duplicates as it goes and gives you a flow of news as it gets voted up elsewhere.</p>
    <p>You can check out the source and make contributions on <%= link_to "Github", "http://github.com/Breccan/Agragr" %>.</p>
	</div>


